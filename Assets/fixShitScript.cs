using UnityEngine;
using System.Collections;

[ExecuteInEditMode]
public class fixShitScript : MonoBehaviour {
	
	// Update is called once per frame
	void Update () {
		transform.position = Vector3.zero;
	}
}
